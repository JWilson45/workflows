name: Build Images

# Single-job reusable workflow: plan tags + docker bake.
# Registry cache only (no local cache) — suitable for ephemeral runners.
# Env-tier attestations + plain progress for stage timings.

on:
  workflow_call:
    inputs:
      images_json:
        required: true
        type: string
      products_json:
        required: false
        default: ""
        type: string
      platforms:
        required: false
        default: linux/amd64
        type: string
      # min = provenance only (fast); max = provenance+sbom (prod); none = no attest
      attest_mode:
        required: false
        default: min
        type: string
      # Registry cache-to mode: min is faster to export; max is better for cold runners
      registry_cache_mode:
        required: false
        default: min
        type: string
      build_runner_label:
        required: false
        default: mm-runner-set
        type: string
      helm_release:
        required: false
        default: ""
        type: string
      kube_namespace:
        required: false
        default: ""
        type: string
      helm_chart:
        required: false
        default: ""
        type: string
      helm_values_file:
        required: false
        default: ""
        type: string
    secrets:
      dockerhub_username:
        required: false
      dockerhub_token:
        required: false

jobs:
  bake:
    name: Bake
    runs-on: ${{ inputs.build_runner_label }}
    timeout-minutes: 60
    concurrency:
      group: ${{ github.workflow }}-${{ inputs.helm_release != '' && inputs.helm_release || 'multi' }}-bake-${{ github.ref }}
      cancel-in-progress: true
    permissions:
      contents: read
      pull-requests: read
      packages: write
      id-token: write
      attestations: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.action == 'closed' && github.event.pull_request.merge_commit_sha || github.sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Plan image builds
        id: plan
        uses: JWilson45/workflows/.github/actions/plan-images@v3.5.3
        with:
          images_json: ${{ inputs.images_json }}
          products_json: ${{ inputs.products_json }}
          do_deploy: "false"
          helm_release: ${{ inputs.helm_release }}
          kube_namespace: ${{ inputs.kube_namespace }}
          helm_chart: ${{ inputs.helm_chart }}
          helm_values_file: ${{ inputs.helm_values_file }}

      - name: Set up Docker Buildx
        if: steps.plan.outputs.should_build == 'true'
        uses: docker/setup-buildx-action@v4
        with:
          driver-opts: network=host

      - name: Log in to image registries
        if: steps.plan.outputs.should_build == 'true'
        shell: bash
        env:
          BUILD_MATRIX: ${{ steps.plan.outputs.build_matrix }}
          REGISTRY_USER: ${{ github.actor }}
          REGISTRY_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          while IFS= read -r registry; do
            [ -z "$registry" ] && continue
            echo "Logging in to $registry"
            echo "$REGISTRY_TOKEN" | docker login "$registry" -u "$REGISTRY_USER" --password-stdin
          done < <(jq -r '.[].registry' <<< "$BUILD_MATRIX" | sort -u)

      - name: Check Docker Hub credentials
        id: dockerhub
        if: steps.plan.outputs.should_build == 'true'
        shell: bash
        env:
          DOCKERHUB_USERNAME: ${{ secrets.dockerhub_username }}
          DOCKERHUB_TOKEN: ${{ secrets.dockerhub_token }}
        run: |
          if [ -n "$DOCKERHUB_USERNAME" ] && [ -n "$DOCKERHUB_TOKEN" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Log in to Docker Hub
        if: steps.plan.outputs.should_build == 'true' && steps.dockerhub.outputs.enabled == 'true'
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.dockerhub_username }}
          password: ${{ secrets.dockerhub_token }}

      - name: Generate bake file and build all images
        if: steps.plan.outputs.should_build == 'true'
        shell: bash
        env:
          BUILD_MATRIX: ${{ steps.plan.outputs.build_matrix }}
          PLATFORMS: ${{ inputs.platforms }}
          ATTEST_MODE: ${{ inputs.attest_mode }}
          REGISTRY_CACHE_MODE: ${{ inputs.registry_cache_mode }}
        run: |
          set -euo pipefail

          image_count="$(jq 'length' <<< "$BUILD_MATRIX")"
          if [ "$image_count" -eq 0 ]; then
            echo "No images to build."
            exit 0
          fi

          platforms_json="$(printf '%s' "$PLATFORMS" | tr ',' '\n' | sed '/^$/d' | jq -R . | jq -s -c .)"
          bake_file="$(mktemp)"

          # Registry cache only — ephemeral runners make type=local cache pure overhead
          # (export at end of job, then disk is thrown away).
          jq -n \
            --argjson images "$BUILD_MATRIX" \
            --argjson platforms "$platforms_json" \
            --arg attest "$ATTEST_MODE" \
            --arg reg_mode "$REGISTRY_CACHE_MODE" \
            '
            def lines:
              if . == null or . == "" then []
              else (split("\n") | map(select(length > 0)))
              end;

            def normalize_cache_to:
              map(
                if startswith("type=registry") then
                  (split(",") | map(select(startswith("mode=") | not)) + ["mode=\($reg_mode)"] | join(","))
                else . end
              );

            def attest_list:
              if $attest == "max" then
                ["type=provenance,mode=max", "type=sbom"]
              elif $attest == "none" then
                []
              else
                ["type=provenance,mode=min"]
              end;

            {
              group: {
                default: {
                  targets: [ $images[].name ]
                }
              },
              target: (
                [ $images[] | {
                    key: .name,
                    value: (
                      {
                        context: .dockerContext,
                        dockerfile: .dockerfile,
                        target: .dockerTarget,
                        tags: (.tags | lines),
                        platforms: $platforms,
                        output: ["type=image,push=true"]
                      }
                      + (if (.cacheFrom | lines | length) > 0
                         then { "cache-from": (.cacheFrom | lines) }
                         else {} end)
                      + (if (.cacheTo | lines | length) > 0
                         then { "cache-to": ((.cacheTo | lines) | normalize_cache_to) }
                         else {} end)
                      + (if (attest_list | length) > 0
                         then { attest: attest_list }
                         else {} end)
                    )
                  }
                ] | from_entries
              )
            }
            ' > "$bake_file"

          echo "Bake definition ($image_count image(s)); attest=$ATTEST_MODE registry_cache=$REGISTRY_CACHE_MODE"
          jq . "$bake_file"

          echo "Building with docker buildx bake (progress=plain for stage timings)..."
          docker buildx bake --progress=plain -f "$bake_file"
